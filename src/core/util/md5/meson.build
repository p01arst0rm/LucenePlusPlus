# liblucene++.util.md5 build file
#============================================================================

liblucene_inc += include_directories('.')
liblucene_util_md5_dir = meson.current_source_dir()


liblucene_src += files(
    join_paths(liblucene_util_md5_dir, 'md5.cpp'))